//Find the length without using length() method
var c = [70,30,69,89,0,2];
console.log(arryLength(c));
function arryLength(){
    var length=0;
    while(c[length]!==undefined){
        length++;
    }
    return length;
}

//output = 6
    
