//The concat() method creates a new array by mergin(concating) existing arrays

const myGirls=["Cecils","Lone"];
const myBoys=["Emil","Tonny","Ronny"];

const myChildren = myGirls.concat(myBoys);
console.log(myChildren);