/**The indexOf() method returns the index of(the position of) the first
 * accurance of a apecified text in a string
 */

const str = "Please locate where locate access";
console.log(str.indexOf("locate"));